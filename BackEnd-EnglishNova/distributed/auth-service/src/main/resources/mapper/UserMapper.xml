<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nightfall.englishnova.auth.mapper.UserMapper">

    <select id="countByUsername" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE username = #{username}
    </select>

    <select id="countByEmail" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE email = #{email}
    </select>

    <select id="countByUsernameExceptId" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE username = #{username} AND id &lt;&gt; #{userId}
    </select>

    <select id="findByAccount" resultType="com.nightfall.englishnova.auth.domain.po.UserPo">
        SELECT id, username, email, avatar_url, password_hash, status
        FROM users
        WHERE username = #{account} OR email = #{emailAccount}
        LIMIT 1
    </select>

    <update id="updateProfile">
        UPDATE users
        SET username = #{username},
            avatar_url = #{avatarUrl}
        WHERE id = #{userId}
    </update>
</mapper>
