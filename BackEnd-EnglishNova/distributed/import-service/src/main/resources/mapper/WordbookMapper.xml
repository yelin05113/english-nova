<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nightfall.englishnova.importservice.mapper.WordbookMapper">

    <update id="syncWordbookCount">
        UPDATE wordbooks
        SET word_count = (
            SELECT COUNT(*)
            FROM vocabulary_entries
            WHERE wordbook_id = #{wordbookId}
        )
        WHERE id = #{wordbookId}
    </update>
</mapper>
