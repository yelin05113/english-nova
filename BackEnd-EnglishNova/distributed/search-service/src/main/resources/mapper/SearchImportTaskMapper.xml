<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nightfall.englishnova.search.mapper.SearchImportTaskMapper">

    <select id="loadImportTaskCleanupRows" resultType="com.nightfall.englishnova.search.domain.vo.ImportTaskCleanupVo">
        SELECT task_id, source_name, error_message
        FROM import_tasks
    </select>

    <update id="updateImportTaskCleanup">
        UPDATE import_tasks
        SET source_name = #{sourceName}, error_message = #{errorMessage}
        WHERE task_id = #{taskId}
    </update>
</mapper>
