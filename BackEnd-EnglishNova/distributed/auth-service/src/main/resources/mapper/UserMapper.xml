<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nightfall.englishnova.auth.mapper.UserMapper">

    <select id="countByUsername" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE username = #{username}
    </select>

    <select id="countByEmail" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE email = #{email}
    </select>

    <select id="countByUsernameExceptId" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE username = #{username} AND id &lt;&gt; #{userId}
    </select>

    <select id="findByUsername" resultType="com.nightfall.englishnova.auth.domain.po.UserPo">
        SELECT id, username, email, avatar_url, quiz_option_strategy, password_hash, status
        FROM users
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="findByEmail" resultType="com.nightfall.englishnova.auth.domain.po.UserPo">
        SELECT id, username, email, avatar_url, quiz_option_strategy, password_hash, status
        FROM users
        WHERE email = #{email}
        LIMIT 1
    </select>

    <update id="updateProfile">
        UPDATE users
        SET username = #{username},
            avatar_url = #{avatarUrl}
        WHERE id = #{userId}
    </update>

    <update id="updateQuizOptionStrategy">
        UPDATE users
        SET quiz_option_strategy = #{quizOptionStrategy}
        WHERE id = #{userId}
    </update>

    <update id="updatePasswordHash">
        UPDATE users
        SET password_hash = #{passwordHash}
        WHERE id = #{userId}
    </update>
</mapper>
